#pragma once
#include <iostream>
#include "glut.h"

using std::string;
using std::pair;
using std::cout;
using std::cin;

double wGrid = 1000;
double hGrid = 800;
double wMenu = 500;
double hMenu = 800;

const double scale = 20;
